# 用途说明：
# 这个文件是 Skill 在 OpenAI/Codex agent 环境下的界面元数据。
# 在 Claude/Cowork 环境中，触发行为由 SKILL.md 的 description 字段控制，本文件不生效。
# 详见 agents/README.md。

interface:
  display_name: "Skill 留存系统"
  short_description: "在创建、迁移或重大更新业务 Skill 之前，先建立原话、事实、决策、产品形态、问题池与迭代记录体系。"
  default_prompt: "请使用 skill-memory-system 为这个 Skill 项目建立留存与迭代文档体系。已存在记忆目录时切换为追加更新模式。"
