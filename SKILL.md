---
name: skill-memory-system
description: "在创建、迁移或重大更新任何业务 Skill 之前，先建立可追溯的项目记忆体系（用户原话、业务事实、产品形态、决策日志、TODO、问题池）。用户说「做一个 Skill」「把这套流程打包成 Skill」「多 agent 协作记录」「保存原话作为一手信息源」时触发。已存在记忆目录时，切换为追加更新模式而非重新创建。反向条件：如果用户只想要一个一次性的、可丢弃的简单 Skill，或者只是临时拼一段 prompt，不激活本系统。"
---

# Skill 留存与迭代系统

在创建或迁移其他 Skill 之前使用本 Skill。它的目标不是替代业务 Skill，而是先搭好一套可追溯、可协作、可迭代的记忆层，让后续 Skill 能通过真实使用逐步变准，而不是只依赖聊天上下文。

## 核心思想

把这些层分开保存，绝不混合：

```text
用户原话    → 一手信息源，原封不动
业务事实    → 已确认的领域事实（分来源，分置信度）
产品形态    → 当前这个 Skill 长什么样（活文档，随对话更新）
决策日志    → 为什么这样设计（有决定、有原因、有影响）
TODO/问题池 → 还没解决什么、什么阻塞了落地
Skill 本体  → 可执行规则
更新日志    → 这个 Skill 如何变化（跟着 Skill 本体走）
```

**最重要的原则：用户原话和 AI 理解必须分开文件存放，永不混写。目录叫什么名字不重要，分层清晰才重要。**

---

## 标准目录

目录命名由使用者自定，没有固定名称要求。建议用能说明用途的名字，例如 `memory/`、`notes/`、`{skill-name}-notes/`，或者直接以创建它的 agent 命名（如 `codex-notes/`、`claude-notes/`）。关键是保持一致，项目内不随意改名。

### 主记忆区（由主导设计的 agent 维护）

```text
{记忆目录}/
  README.md          ← 项目概览和协作规则
  用户原话.md         ← 一手信息源，只记录用户说的
  业务事实.md         ← 已确认事实（区分来源和置信度）
  Skill产品形态.md    ← 活文档，Skill 当前长什么样
  决策日志.md         ← 设计决策和依据
  TODO.md            ← 任务状态
  问题池.md           ← 待解答问题（分优先级）
  协作说明.md         ← 多 agent 时使用
```

> 各文件的 **Markdown 模板** 见 [`references/templates.md`](references/templates.md)。本 SKILL.md 只说规则，不再重复模板。

### 审查区（条件触发，默认不创建）

**只有当用户明确表示「我要用两个 AI 来做这个 Skill」「让另一个 agent 也参与」「需要第二视角审查」时，才创建审查区。** 用户没有说明时，不主动询问，不自动创建。

审查区被激活后，目录名由第二个 agent 自定（如 `claude-review/`、`review/`），内含：独立理解笔记、需用户确认的问题、实现建议、对主方案的审阅意见。

**两个区角色对等，不是从属关系。** 主记忆区负责设计推进，审查区负责独立质疑和补充盲区。任何分歧都必须明确记录，由用户拍板，不得私下消除。

**退出审查模式：** 若用户后续不再需要审查区（如明确说「只让一个 agent 继续」），将审查目录原地保留作为历史档案，不再追加更新，主记忆区独立推进即可。

---

## 各文件的规则（不是格式）

格式见 `references/templates.md`。这里只说写入规则与红线。

### 用户原话.md

- 一手信息源，发生分歧时是唯一公正依据。
- **不总结，不改写，不修正语气或语法，不混入 AI 判断或推断。**
- 只做日期、编号（U001 / U002 ...）、简短标题三种标注。
- 用户每次补充新信息，追加新编号，不覆盖旧内容。
- **多 agent 时编号分配规则：** 主记忆区维护者负责追加新编号，审查区只读不写。如果用户直接对审查区 agent 说话，由审查区 agent 把原话提交给主记忆区维护者再写入。

### 业务事实.md

- 必须分四个区：「来自用户的一手事实」「来自源文件的事实」「AI 推断（标置信度）」「已被修正的事实」。
- **禁止：** 把 AI 推断直接写入「已确认」区，不加置信度标注。
- 用户纠正后，错误事实移入「已被修正」区，**保留划线原文 + 修正出处**，不删除。

### Skill产品形态.md

- 活文档，不是最终 PRD。回答：是什么 / 服务谁 / 输入输出 / 核心工作流 / 当前模块 / 质量红线 / 未定问题。
- **质量红线举例：** 「主播说每一句话都要被引用，不允许 AI 编造」「输出必须有时间戳」「不允许把推断写成事实」。
- 每次更新在文件顶部更新时间戳，不删除历史版本（追加或标注变化）。

### 决策日志.md

- 三段式：决定 / 原因 / 影响。
- **何时写：** 只要某个判断会影响 Skill 的结构、触发、输出、质量门或迭代方式，就写入。
- **不要写：** 临时操作步骤、还没确定的想法（放问题池）。

### TODO.md

- 按状态分区：已完成 / 待处理 / 等用户回答 / 等真实样本验证。**不要把不同状态混在一起。**

### 问题池.md

- 按优先级分层：🔴 阻塞级 / 🟡 重要级 / 🟢 后续级 / ✅ 已解答。
- 已解答的问题标 ✅ 并写明答案，不删除原问题，保留演进历史。
- **阻塞级问题未解答时，不要推进依赖该问题的设计决策。**

### 协作说明.md（仅多 agent 时使用）

- 写清楚：角色分工 / 文件所有权 / 分歧处理 / 信息同步。
- **分歧本身是有价值的，不是需要消除的噪音。** 由用户拍板，不得私下「协商」消除。

---

## Skill 内部更新日志（UPDATE_LOG.md）

**位置：在业务 Skill 文件夹内，跟着 Skill 本体走，不放在记忆目录里。**

每次因真实样本或用户反馈导致修改时追加记录，包含：来源 / 问题 / 修改 / 影响 / 待验证点。完整模板见 `references/templates.md`。

创建业务 Skill 时，在其 `SKILL.md` 末尾加入「迭代记录」一节，引导维护者在以下情况更新 UPDATE_LOG.md：用户纠正了判断 / 出现新高频案例 / 某字段经常缺失 / 某规则被真实样本证伪 / Manifest 需新增字段。

---

## 跨 agent 适配说明（agents/ 目录）

本 Skill 设计为跨 agent 平台可用。`agents/` 目录存放各平台的**界面元数据**（如 OpenAI/Codex 用 `openai.yaml`、其他平台对应文件），用于在该平台 UI 上显示 Skill 名称、简介、默认 prompt。

**重要：触发逻辑只由 SKILL.md 的 `description` 控制。** `agents/` 下任何文件都不影响 Claude/Cowork 环境的触发判断，仅在对应平台展示用。新增平台时，在 `agents/` 下加一个对应文件即可，不要改动 SKILL.md 的触发逻辑。

---

## 标准工作流

1. **创建记忆目录** — 工作目录旁边新建，命名自定。已存在则切换为追加模式，不覆盖。审查区默认不创建。
2. **写入用户原话** — 把触发本次的原始表达写入 `用户原话.md`，U001 编号 + 简短标题 + 引用原话，不总结。
3. **提取业务事实** — 从原话提取已确认事实到「来自用户的一手事实」区；读源文件到「来自源文件的事实」区；AI 推断单独区，标置信度。
4. **起草产品形态** — 回答：是什么 / 服务谁 / 输入输出 / 核心工作流 / 未定问题。活文档，后续持续追加。
5. **写入第一批决策** — 已形成的设计判断写入 `决策日志.md`；未定的判断进 `问题池.md`。
6. **建立 TODO 和问题池** — 阻塞级问题未解答前，不推进依赖该问题的决策。
7. **创建或更新业务 Skill** — 业务 Skill 进自己的文件夹，不放在记忆目录里。
8. **在业务 Skill 内创建 UPDATE_LOG.md** — 记录第一版的来源、初始假设、待验证点。
9. **持续维护** — 用户每次补充新信息 → 追加原话 → 更新事实 → 更新产品形态 → 视情况更新决策日志和 TODO。问题被解答标 ✅，事实被纠正移入「已被修正」区，都不删除。

---

## 使用边界

- 记忆文档不是 Skill 本体，业务 Skill 仍应保持简洁、可执行。
- 早期假设不要直接写成稳定规则，先放在「AI 推断」区或「等真实样本验证」区。
- 不覆盖其他 agent 的文件夹，除非用户明确要求。
- **用户原话必须和 AI 理解分开文件，永不混写。**
- 只有稳定、通用、需要随 Skill 分发的内容，才进入业务 Skill 文件夹。
- 已有记忆目录时切换为追加更新模式，不重新创建覆盖。
- 记忆目录叫什么名字不重要，分层清晰、不混写才重要。

---

## 想看完整示例？

`examples/` 目录下有最小可用样本，演示一个真实项目的 用户原话.md / 业务事实.md / 决策日志.md 长什么样。
